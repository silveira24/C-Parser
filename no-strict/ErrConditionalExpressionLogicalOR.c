int main () {
  
  int num;

  printf("Informe um numero: ");
  scanf("%d", &num);

  int num2;
  num2 = (num < 0) ? 0 : ;
  //num >= 0 ? num++ : num--;

  printf("O valor de num2 e: %d \n\n", num2);

  return 0;
}

